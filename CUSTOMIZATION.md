# Customization Guide

Botanical Blue uses a token-based design system. All visual attributes can be modified by changing CSS variables.

## Colors & Spacing
Edit `styles/tokens.css` and `styles/colors.css` to change the overall theme.

## Motion
Edit `styles/animations.css` and `lib/motion` to change easing functions and durations.
